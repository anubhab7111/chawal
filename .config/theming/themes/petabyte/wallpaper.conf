# One entry per line: "<output>:<path>", or "*:<path>" to apply to all outputs.
# Applied with `awww img` by apply-theme.sh — awww plays animated GIFs natively,
# no extra flags needed.
# Drop your wallpaper file at this path (an animated .gif). Not present yet —
# apply-theme.sh will warn and skip until you do.
*:/home/ushtro/Pictures/wallpapers/petabyte/wallpaper.gif
